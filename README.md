# Localhosters — HackAlem AI

Кейс: **Agentic AI для прогнозирования почасовой выработки ВЭС** на горизонте 24–48 часов.

Система должна сама проходить полный цикл: получить прогноз погоды из открытых источников → подготовить данные → запустить модель → выдать почасовой прогноз → проанализировать результат → пересчитать при обновлении входных данных. Проверка — бэктест за февраль 2026 с использованием **архивных** прогнозов погоды, доступных на момент каждого прогноза.

## Статус

🔬 **Ресёрч.** Изучаем источники архивных прогнозов, данные SCADA и архитектуру агента. План и задачи — в [`docs/research/`](docs/research/README.md).

SCADA интерпретируется как фиксированный UTC+6 на всём периоде: помесячный пик плавает 5.2–6.7 ч, вывод по pooled-окнам. Временные метки локализуются через `Etc/GMT-6`, а не `Asia/Almaty`; воспроизводимое доказательство находится в `scripts/tz_check.py` и `docs/figures/tz_xcorr.png`.

## Метрика, которую надо побить (LOC-9)

Честный case-compliant бейзлайн Previous Runs `ws100` → refit-кривая даёт **MAE 0.20–0.22** по нормализованной мощности (h1–24) и около **0.22** на h25–48. Цель модели — MAE ≤0.19 (skill ≥10%), приемлемый результат — ≤0.22. ERA5 и будущий SCADA-ветер в эти бейзлайны не входят: это недоступные на issue-time потолки. Воспроизводимые таблицы: [`docs/research/baselines.md`](docs/research/baselines.md), полный CSV: [`reports/baselines.csv`](reports/baselines.csv).

## Команда

| Кто | Направление |
|---|---|
| Akylbek | Данные SCADA и ML-модель |
| Ramazan Kazybek | Источники погодных прогнозов, пайплайн, бэктест |
| Birzhan Abishev | Агент, продукт, README, демо |

## Структура

```
task context/     условие кейса и датасет организаторов (2 турбины, 10-мин SCADA, 2023-03 → 2026-01)
docs/research/    план ресёрча и его результаты
```

Разделы «Архитектура», «Установка», «Запуск», «Параметры окружения» и «Проверка основного сценария» появятся после фазы ресёрча.

## Архив прогнозов (LOC-14)

Клиент Open-Meteo, 116 запусков ECMWF IFS за 31.01–28.02.2026, переносимый
JSON-кеш и Parquet готовы к локальной работе. Установка, запуск, схема и
ограничения данных: [docs/03_WEATHER_ARCHIVE.md](docs/03_WEATHER_ARCHIVE.md).
