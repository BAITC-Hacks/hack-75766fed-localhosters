# LLM-планировщик на окне `test`

Модель: `gpt-5.4-mini` (OpenAI, Pydantic AI). Прогоны: [`runs/llm/test/`](../runs/llm/test).

| Показатель | Значение |
|---|---|
| Решений LLM | 58 |
| Совпало с детерминированными правилами | 58 из 58 |
| Откатов на правила (ошибка или непроверяемый ответ LLM) | 0 |
| Токены, вход / выход | 69,956 / 9,465 |
| В среднем на решение | 1,206 / 163 |
| Стоимость | $0.095 (≈ $0.0016 за решение; $0.75/$4.5 за 1M токенов) |

Прогноз мощности в обоих режимах один и тот же (его считает модель, а не LLM). LLM решает, публиковать ли выпуск и нужен ли пересчёт, и пишет объяснение для диспетчера; ссылаться он может только на ран, прошедший проверку доступности, иначе выпуск откатывается на правила.

## Расхождения с правилами

Нет.

## Примеры объяснений LLM

- `2026-01-31T1800Z-dayahead` · `initial_issue` — Публикуем первый выпуск прогноза по доступному NWP‑запуску 2026-01-31T06:00:00Z. Предыдущего выпуска нет, поэтому измеренные расхождения ветра и дельта мощности отсутствуют.
- `2026-01-31T2000Z-reissue` · `new_nwp_run` — Новый прогон НМП изменил прогноз, публикация нужна. Расхождение по ветру 0.19 м/с при пороге 1.5 м/с, изменение мощности незначительное.
- `2026-02-01T1800Z-dayahead` · `new_nwp_run` — Качество данных пройдено, выпуск разрешён по новому NWP-рану. Расхождение ветра 0.263 м/с, дельта мощности 0.058; при старом выпуске это ниже порога и требует новой версии только из‑за нового запуска.
- `2026-02-01T2000Z-reissue` · `new_nwp_run` — Новая версия прогноза публикуется по новому NWP запуску. Расхождение по ветру 0.33 м/с, изменение по мощности 0.07; порог качества пройден.
